; The 1st of comment test
;The 2nd of comment test

fn func(a: i32): i32 =>
	let b: i32 = 1
	let c: i32 = 2
	return (3 * 4) - (a / 1) + b

fn func_(a: i32): i32 =>
	func (1 + 7)
	return 1 + 1

fn func__(a: i32): i32 =>
	return 1

; The 3rd of comment test
pub fn main: i32 => ; comment 4th
	; return func__ (1 + 2)
	return func (func 1) ; The 5th
