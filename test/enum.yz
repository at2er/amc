struct A =>
	i: i32
	a: i32
	mut b: i32

enum Bool: i32 => true | false

; enum B: A =>
; | default =>
; 	i = 0
; 	a = 0
; 	b = 0
; | err =>
; 	i = 1
; 	b = 1

pub fn main: i32 =>
	let a: Bool = Bool.false
	let b: B = { 2, 1, null }
	; match b
	; | default => ret 1
	; | err => ret 2
	return a
